# Arquitectura SEO — supein.club

> Snapshot: 2026-09-16 (pillars food, football y culture actualizados;
> travel/living recibieron solo un fix de rutas compartido, sin cambio
> de contenido; language conserva el snapshot 2026-09-09 original — ver
> notas de cada sección). Los volúmenes de búsqueda son rangos redondeados
> del Keyword Planner de Google Ads. Tres fuentes, ninguna trackeada en
> git (viven sueltas en la raíz del repo):
> - `SupeinClub_Arquitectura_SEO.xlsx` — árbol maestro Nivel 1/2/3 por
>   keyword, fuente principal de este documento.
> - `supeinclub_50_articulos_SEO.docx` — plan editorial de 50 artículos
>   "basado en" el xlsx anterior; usado como confirmación secundaria.
> - `Keywords_Fútbol.csv` — único research dedicado al pillar football,
>   no cubierto por el xlsx maestro.
>
> No son cifras exactas ni en tiempo real — revisar periódicamente. Donde
> no existe research documentado, la celda dice explícitamente **N/D**,
> nunca una cifra inventada.
>
> Este inventario es el estado REAL construido (rutas verificadas contra
> `src/pages/` y las colecciones `articles`/`spain-map`), no el plan. Las
> tres fuentes usan **tres esquemas de slugs distintos entre sí y
> distintos del sitio real** (p. ej. el xlsx llama al pillar language
> `/spanish-language/`, el docx lo llama `/language/lessons/...`, el
> sitio real lo sirve en `/language/`). El cruce entre fuentes y URLs
> reales se hizo por **coincidencia de keyword/intención**, no de ruta.
> Donde hay divergencia relevante, se anota bajo la tabla del pillar.

## Por pillar: URLs reales vs. planificadas

### travel

| URL real | Estado | Keyword principal | Volumen (bucket) | Fuente del volumen |
|---|---|---|---|---|
| `/travel/` | publicado (hub) | スペイン 旅行 | 5k | xlsx (HOME) |
| `/travel/barcelona/` | publicado (listado de clúster, no artículo dedicado) | バルセロナ 観光 | 5k (nodo Nivel3 `/barcelona/`: 15k) | xlsx |
| `/travel/barcelona/barcelona-travel-guide/` | publicado (artículo) | — | N/D | N/D |
| `/travel/barcelona/sagrada-familia-guide/` | publicado (artículo) | — | N/D | N/D |
| `/travel/barcelona/sagrada-familia-jesus-tower-completed/` | publicado (artículo) | — | N/D | N/D |
| `/travel/madrid/` | publicado (listado de clúster, no artículo dedicado) | マドリード 観光 | 5k (nodo Nivel3 `/madrid/`: 15k) | xlsx |
| `/travel/madrid/madrid-travel-guide/` | publicado (artículo) | — | N/D | N/D. Ampliado 2026-09-16 con 2 museos secundarios (Sorolla, Cerralbo) y el Barrio de Salamanca, a partir del análisis de una guía turística comercial escaneada (`Escaneo/informe_completo_Madrid.md`). **Regla aplicada**: los restaurantes con nombre propio de esa fuente NO se añadieron aquí — fueron a `/food/spain-map/madrid/` (travel=qué visitar, food=dónde comer, regla ya documentada abajo); los tablaos concretos fueron a `/culture/flamenco/` (concentra la autoridad temática "フラメンコ" en una sola página en vez de fragmentarla) |
| `/travel/spain-tours/` | publicado (listado de clúster, no artículo dedicado) | スペイン ツアー | **500k** (nodo `/tours/` ★: 515k) | xlsx + docx (ambos coinciden) — pilar obligatorio del plan, nunca se escribió como artículo dedicado |
| `/travel/spain-tours/budget-spain-tours/` | publicado (artículo) | — | N/D | N/D |
| `/travel/spain-tours/spain-tour-1week/` | publicado (artículo, con CRE Rankings) | — | N/D | N/D |
| `/travel/travel-cost/` | publicado (listado de clúster, no artículo dedicado) | スペイン 旅行 費用 | 5k (nodo Nivel3 `/travel-cost/`: 15k) | xlsx |
| `/travel/travel-cost/spain-budget-solo/` | publicado (artículo) | — | N/D | N/D |
| `/travel/andalusia/` | stub, sin contenido | アンダルシア 観光 | 5k (nodo Nivel3 `/andalusia/`: 15k) | xlsx |
| `/travel/best-season/` | stub, sin contenido | スペイン 旅行 ベストシーズン | 5k (nodo Nivel3 `/best-time-to-visit/`: 10k) | xlsx |
| `/travel/canarias/` | stub, sin contenido | — | N/D | N/D — no aparece en ninguna de las 3 fuentes |
| `/travel/ibiza/` | stub, sin contenido | スペイン イビサ 島 ★NEW | 500 (nodo Nivel3 `/ibiza/`: 1.5k) | xlsx |
| `/travel/itineraries/` | stub, sin contenido | スペイン 旅行 日程 | 5k (nodo Nivel3 `/itineraries/`: 15k) | xlsx |
| `/travel/spain-flights/` | stub, sin contenido | スペイン 航空券 | 5k (nodo Nivel3 `/flights/`: 15k) | xlsx |
| `/travel/spain-hotels/` | stub, sin contenido | スペイン ホテル | 5k (nodo Nivel3 `/hotels/`: 15k) | xlsx |
| `/travel/souvenirs/` | publicado (artículo, insignia) — 2026-09-16 | スペイン お土産 | 5k (nodo Nivel2 `/spanish-food/souvenirs/` ★NUEVA: 20k, originalmente planificado bajo food) | xlsx (reubicado) — artesanía y marcas por región (Lladró/Valencia, damasquinado/Toledo, abanicos/Sevilla, Loewe, Camper y Carmina/Mallorca, El Caganer/Cataluña) a partir del análisis de una revista escaneada. Sin `region` (multi-regional); `relatedFood` en Toledo, Sevilla, Valencia, Islas Baleares, Barcelona y Guipúzcoa. Sustituye y elimina el antiguo stub `/food/souvenirs/` — ver nota en la tabla food |

**Nota travel:** el nodo `/travel-spain/booking/` (★PRIORITARIA en el xlsx, 515k agregado) agrupa tours+vuelos+hoteles como intención transaccional — hoy repartido en 3 stubs vacíos (`spain-flights`, `spain-hotels`) más el clúster `spain-tours` con 2 artículos reales. Los 4 "pilares obligatorios" que el plan quería como artículos dedicados en `/travel/barcelona/`, `/travel/madrid/`, `/travel/spain-tours/`, `/travel/andalusia/` nunca se escribieron así — 3 de esas URLs sirven listados automáticos de clúster, y `andalusia` sigue vacío.

### food

| URL real | Estado | Keyword principal | Volumen (bucket) | Fuente del volumen |
|---|---|---|---|---|
| `/food/` | publicado (hub) | スペイン 料理 | 5k | xlsx (HOME) + docx — categorías reorganizadas 2026-09-16: 地方グルメルート (food-routes), 名物料理・食材ガイド (paella, jamon-iberico), スペインワイン (spanish-wine), バル・レストラン文化 (tapas, spain-bar-restaurant, spain-dining-times), グルメ語彙 (spanish-vocabulary). Antes solo existía 1 categoría y 13 de los 14 artículos caían en un cajón "その他"; ahora el cajón queda vacío. Al añadir un cluster nuevo, decidir si encaja en una categoría existente o necesita una nueva |
| `/food/food-routes/{7 rutas}/` | publicado (7 artículos) | — | N/D | N/D — keywords propios de esta sesión (combos región+plato), sin research de Keyword Planner. Enriquecidos 2026-09-16 con menciones a rabo de toro/Córdoba (`sur-fritos-flamenco`) y butifarra/zarzuela (`este-paella-tapas`), huecos detectados al analizar el informe de bar/restaurante |
| `/food/jamon-iberico/` | publicado (artículo, insignia) | ハモン・イベリコ | N/D | N/D — creado 2026-09-14 a partir de material promocional de ASICI (evento sectorial en Tokio); no aparece en xlsx/docx. `region: "extremadura"` en frontmatter → aparece en `/regions/extremadura/`; `relatedFood` en Cáceres y Badajoz → tarjeta relacionada en ambas páginas de provincia |
| `/food/paella/` | publicado (artículo, insignia) | パエリア | 5k (nodo Nivel3 `/paella/`: 15k) | xlsx — coincide con el nodo planificado, pero el ángulo real cubre historia+格付け+técnica+レシピ, más una sección de E-E-A-T sobre el jurado del concurso nacional de paella de Japón (no solo "レシピ" como decía el título del stub original). `region: "comunidad-valenciana"` → `/regions/comunidad-valenciana/`; `relatedFood` en Valencia |
| `/food/spanish-wine/` | publicado (artículo, insignia) | スペインワイン | 5k (nodo Nivel3 `/wine/`: 15k) | xlsx — historia, clasificación DO/DOCa/crianza, 11 regiones, turismo enológico. Sin `region` (multi-regional por naturaleza, ninguna de las 8 regiones de `/regions/` domina); `relatedFood` en La Rioja, Barcelona y Cádiz (sedes del turismo enológico cubierto) |
| `/food/tapas/` | publicado (artículo, insignia) | タパス とは（文化・使い方） | 5k (nodo Nivel3 `/tapas/`: 15k) | xlsx — **divergencia de ángulo**: el nodo planificado era "タパス レシピ" (recetas); el artículo real cubre cultura del tapeo, tapa/ración/pincho y una selección curada de 10 tapas, para no competir con el glosario exhaustivo de `spanish-vocabulary` |
| `/food/spain-bar-restaurant/` | publicado (artículo, insignia) | スペイン バル レストラン マナー | N/D | N/D — cluster nuevo, no existe en xlsx/docx/csv; nace del análisis de una revista escaneada (bar/restaurante, etiqueta). **No confundir con el hueco `spanish-bars` de la nota food de abajo** — ese es バル東京 (bares españoles EN Japón, intención local); este es la etiqueta de bares EN España (intención de viajero), sin solapamiento |
| `/food/spain-dining-times/` | publicado (artículo, insignia) | — | N/D | N/D |
| `/food/spanish-vocabulary/` | publicado (artículo, insignia) | — | N/D | N/D — ampliado 2026-09-16 de ~180 a ~200 términos (café, sumiller, mosto, calimocho, butifarra, zarzuela, etc.) |
| `/food/spain-map/` | publicado (hub bespoke, no colección `articles`) | スペイン 地図 / 県別レストラン (concepto interno de esta sesión) | N/D | N/D — sin Keyword Planner |
| `/food/spain-map/{53 provincias}/` | publicado (colección `spain-map`, 53 páginas) | ver caso especial abajo | N/D | N/D |
| `/food/spain-map/[province]/[article]/` | ruta preparada, **0 páginas generadas hoy** | — | — | — |
| `/food/japan-map/` | publicado (hub bespoke, no colección `articles`) | — | N/D | N/D |
| `/food/olive-oil/` | stub, sin contenido | スペイン オリーブオイル | 5k (nodo Nivel3 `/olive-oil/`: 10k) | xlsx |
| `/food/restaurants-osaka/` | stub, sin contenido | スペイン レストラン 大阪 | 5k (nodo Nivel3 `/osaka/`: 10k) | xlsx |
| `/food/restaurants-tokyo/` | stub, sin contenido | スペイン レストラン 東京 | 5k (nodo Nivel3 `/tokyo/`: 15k, incluye バル東京) | xlsx |
| `/food/souvenirs/` | **eliminado 2026-09-16** — ver `/travel/souvenirs/` | — | — | El xlsx lo situaba bajo food, pero David decidió que el contenido de souvenirs encaja mejor en travel (intención de compra/viaje, no gastronómica). El stub (sin contenido, noindex) se borró en vez de dejarlo huérfano; no había nada indexado que redirigir |

**Nota food:** el nodo `/restaurants-in-japan/` del xlsx incluye スペイン バル 東京 (5k) dentro del mismo total que レストラン東京 — el plan docx lo separaba en un satélite propio (`spanish-bars`) que nunca se construyó como stub independiente.

**Interlinking hub-and-spoke (food, 2026-09-16):** los artículos insignia recientes (`jamon-iberico`, `paella`, `spanish-wine`, `spain-bar-restaurant`, `tapas`) están enlazados entre sí y con los 7 `food-routes` en ambas direcciones, evitando duplicar contenido: cada insignia cubre el ángulo profundo/técnico de su tema, mientras que los `food-routes` mantienen su ángulo regional/de maridaje y enlazan de vuelta al insignia correspondiente para quien quiera profundizar. `spanish-vocabulary` funciona como glosario exhaustivo al que todos enlazan en vez de repetir listas de vocabulario.

**Asociación a provincia y a `/regions/` (mecanismo, no solo food):** dos piezas de datos independientes conectan un artículo con la geografía:
1. `region: "<slug>"` en el frontmatter del artículo (slugs en `src/config/regions.ts`) → el artículo aparece automáticamente en el bloque "料理" de `/regions/{slug}/`. Solo se define si el artículo tiene una región dominante clara (paella→comunidad-valenciana, jamón ibérico→extremadura); se deja sin definir si es multi-regional (spanish-wine).
2. `relatedFood: ["/food/..."]` en el frontmatter de una entrada de la colección `spain-map` → hace aparecer una tarjeta con título/descripción reales en `/food/spain-map/{provincia}/`, resuelta dinámicamente contra la colección `articles` en `ProvinceRelated.astro`. **Este mecanismo llevaba tiempo roto** (el prop se recibía pero nunca se renderizaba, por colisión entre el icono `Map` de lucide-astro y el `Map` nativo de JS) — corregido 2026-09-15.
Desde el 2026-09-15, toda creación de artículo de food pregunta explícitamente si debe vincularse a alguna provincia/región concreta (ver memoria de sesión `feedback_ask_province_linking`).

### language

| URL real | Estado | Keyword principal | Volumen (bucket) | Fuente del volumen |
|---|---|---|---|---|
| `/language/` | publicado (hub) | スペイン語 | 5k | xlsx (HOME) |
| `/language/learn-spanish/` | publicado (artículo, insignia) | スペイン語 勉強 | 5k (nodo Nivel2 `/learn-spanish/`: 25k) | xlsx + docx (única coincidencia exacta de ruta plan↔real entre las 3 fuentes) |
| `/language/learn-spanish/{A1–C2, DELE, guías}/` (52 páginas) | publicado (artículos) | — | **N/D — construido desde PCIC, no desde keyword research** | — |
| `/language/learn-spanish/dele-vs-seiken/` | publicado (artículo) | DELE 西検 違い / どちら | N/D | N/D — ver nota DELE abajo |
| `/language/learn-spanish/dele-oral-interview-guide/` | publicado (artículo) | DELE面接 / 口頭試験対策 | N/D | N/D — ver nota DELE abajo |
| `/language/learn-spanish/spanish-interview-questions-self-pr/` | publicado (artículo) | スペイン語面接 質問 / 自己PR | N/D | N/D — ver nota DELE abajo |
| `/language/learn-spanish/spanish-interview-practice/` | publicado (artículo) | スペイン語 模擬面接 | N/D | N/D — ver nota DELE abajo |
| `/language/learn-spanish/dele-difficulty-guide/` | publicado (artículo) | DELE 難易度 | N/D | N/D — ver nota DELE abajo |
| `/language/tokyo-spanish-schools/` | publicado (artículo, insignia) | スペイン語 教室 東京 | 5k (nodo Nivel3 `/schools-in-japan/`: 15k) | xlsx |
| `/language/osaka-spanish-schools/` | publicado (artículo) | 大阪 スペイン語 教室 | N/D | N/D — ranking CRE, no keyword research |
| `/language/yokohama-spanish-schools/` | publicado (artículo) | 横浜 スペイン語 教室 | N/D | N/D — ranking CRE, no keyword research |
| `/language/kyoto-spanish-schools/` | publicado (artículo) | 京都 スペイン語 教室 | N/D | N/D — ranking CRE, no keyword research |
| `/language/lesson-price/` | stub, sin contenido | スペイン語 レッスン 料金 | 5k (nodo Nivel3 `/pricing/`: 15k) | xlsx |
| `/language/online-lessons/` | stub, sin contenido | スペイン語 オンライン レッスン | 5k (nodo Nivel3 `/online-lessons/`: 15k) | xlsx |
| `/language/pronunciation/` | stub, sin contenido | スペイン語 発音 | 5k (nodo Nivel3 `/pronunciation/`: 15k) | xlsx (ruta exacta) |
| `/language/self-study/` | stub, sin contenido | スペイン語 独学 | 5k (nodo Nivel3 `/self-study/`: 15k) | xlsx |
| `/language/spanish-apps/` | stub, sin contenido | スペイン語 アプリ | 5k (nodo Nivel3 `/apps/`: 15k) | xlsx |
| `/language/spanish-books/` | stub, sin contenido | スペイン語 本 | 5k (nodo Nivel3 `/books/`: 15k) | xlsx |
| `/language/spanish-phrases/` | stub, sin contenido | スペイン語 フレーズ | 5k (nodo Nivel3 `/phrases/`: 15k, incluye 挨拶+旅行フレーズ) | xlsx |

**Nota DELE (silo面接/難易度, 2026-09-09):** `dele-vs-seiken`, `dele-oral-interview-guide`, `spanish-interview-questions-self-pr`, `spanish-interview-practice` y `dele-difficulty-guide` no vienen de ninguna de las 3 fuentes de este documento (xlsx/docx/csv) ni de PCIC — nacen de un análisis competitivo ad-hoc sobre Instituto Cervantes Tokio y de un listado de preguntas japonesas frecuentes en buscadores, ambos aportados fuera del flujo de Keyword Planner. Se enlazan entre sí y con `dele/index` para formar un silo interno sin solapar intención (examen DELE vs. entrevista de trabajo general vs. dónde practicar vs. dificultad por nivel). **`dele-difficulty-guide` reemplaza la idea original de un "hub de tasas de aprobación"**: se descartó tras verificar que ni el Instituto Cervantes ni el organismo del 西検 (公益財団法人日本スペイン協会) publican 合格率 oficiales por nivel — el artículo lo declara explícitamente en vez de estimar cifras. Por el mismo motivo, `dele-vs-seiken` tuvo sus cifras de precio (ya publicadas, no verificables) retiradas y sustituidas por enlaces a las webs oficiales.

### living

| URL real | Estado | Keyword principal | Volumen (bucket) | Fuente del volumen |
|---|---|---|---|---|
| `/living/` | publicado (hub) | スペイン 生活 | 5k | xlsx (HOME) |
| `/living/cervantes-webinar-experiencias-espana/` | publicado (artículo, insignia) | — | N/D | N/D |
| `/living/i-need-spain/{8 episodios}/` | publicado (artículos) | — | N/D | N/D — serie de entrevistas, no formaba parte de ningún plan de keywords |
| `/living/language-school/` | stub, sin contenido | スペイン 語学学校 | 5k (nodo Nivel3 `/language-schools/`: 15k) | xlsx |
| `/living/spain-visa/` | stub, sin contenido | スペイン ビザ | 5k (nodo Nivel2 `/visa/`: 15k) | xlsx |
| `/living/student-visa/` | stub, sin contenido | スペイン 学生ビザ | 5k (nodo Nivel3 `/student-visa/`: 15k) | xlsx |
| `/living/study-cost/` | stub, sin contenido | スペイン 留学 費用 | 5k (nodo Nivel3 `/cost/`: 15k) | xlsx |
| `/living/study-spain/` | stub, sin contenido | スペイン 留学 | 5k (nodo Nivel2 `/study/`: 20k) | xlsx |
| `/living/university/` | stub, sin contenido | スペイン 大学 | 5k (nodo Nivel3 `/universities/`: 15k) | xlsx |
| `/living/work-in-spain/` | stub, sin contenido | スペイン 就職 | 5k (nodo Nivel3 `/jobs/`: 15k) | xlsx |
| `/living/work-visa/` | stub, sin contenido | スペイン 就労ビザ | 5k (nodo Nivel3 `/work-visa/`: 15k) | xlsx |
| `/living/working-holiday/` | stub, sin contenido | スペイン ワーホリ | 5k (nodo Nivel3 `/working-holiday/`: 15k) | xlsx |

### culture

| URL real | Estado | Keyword principal | Volumen (bucket) | Fuente del volumen |
|---|---|---|---|---|
| `/culture/` | publicado (hub) | スペイン 文化 | 500 | xlsx (HOME) |
| `/culture/facts/` | stub, sin contenido | スペイン 基本情報 | 500 (nodo Nivel2 `/about-spain/facts/`: 1.5k) | xlsx |
| `/culture/flamenco/` | publicado (artículo, insignia) — 2026-09-16 | フラメンコ | 500 (dentro del nodo `/about-spain/culture/`: 1.5k) | xlsx — coincide con el nodo planificado, pero **el ángulo real es distinto al título SEO original del shell**: el shell prometía "東京で見られる公演情報・教室情報" (dónde verlo/aprenderlo en Tokio); el artículo real se centra en España (historia, 3 pilares cante/guitarra/baile, tablao vs. teatro, etiqueta de observación), por no disponer de fuente fiable sobre la oferta en Tokio — título/descripción del shell ajustados para no prometer ese ángulo. `region: "andalucia"`; `relatedFood` en Sevilla, Granada y Cádiz |
| `/culture/history/` | stub, sin contenido | スペイン 歴史 | 500 (nodo Nivel2 `/about-spain/history/`: 1k) | xlsx + docx |
| `/culture/traditions/` | publicado (artículo, insignia) — 2026-09-16 | スペインの祭り | 500 (nodo Nivel2 `/about-spain/traditions/`: 1.5k, incluye お祭り+トマティーナ) | xlsx — coincide con el nodo planificado. Calendario de 18 fiestas (Fallas, San Fermín, La Tomatina, etc.) a partir del análisis de una revista escaneada. Sin `region` (multi-regional); `relatedFood` en las 17 provincias directamente implicadas (guipuzcoa, cadiz, santa-cruz-de-tenerife, valencia, alicante, cordoba, madrid, toledo, granada, navarra, la-coruna, malaga, la-rioja, barcelona, zaragoza, sevilla, cuenca) |

**Culture pasa de 0 a 2 páginas publicadas (2026-09-16)** — `/culture/traditions/` y `/culture/flamenco/` son los dos primeros artículos reales del pillar; `facts` e `history` siguen siendo stubs. Es también el pillar de menor volumen agregado de las 3 fuentes (~3k total vs. 500k+ de travel/football) — coherente con ser contenido de refuerzo de marca, no de captación.

**Guardrail a futuro (`/culture/history/`):** `traditions.mdx` menciona de pasada una recreación histórica (Moros y Cristianos) y `flamenco.mdx` cubre su propia historia (origen s. XV, cafés cantantes, etc.) sin invadir el terreno de una futura guía general de historia de España. Cuando `/culture/history/` se desarrolle, mantener esas menciones existentes breves y enlazar hacia él en vez de profundizar ahí.

**Ampliación 2026-09-16 — tablaos concretos:** a partir del mismo análisis de la guía de Madrid, se añadió una sección "マドリードのおすすめタブラオ" (Corral de la Morería, Tablao Flamenco 1911, Torres Bermejas) a `flamenco.mdx`, en vez de a `madrid-travel-guide.mdx` — decisión deliberada para concentrar toda la intención de búsqueda "フラメンコ" (incluida "マドリード フラメンコ タブラオ") en la página que ya es la autoridad temática del sitio, en lugar de fragmentarla entre pillars. `region: "andalucia"` se mantiene (representa la cuna cultural, no una limitación del contenido práctico) — mismo criterio que `spanish-wine.mdx` sin `region` pese a cubrir 11 regiones.

**Estudio de canibalización previo a `flamenco.mdx` (2026-09-16):** de las 13 menciones de "flamenco" en el sitio, ninguna era un desarrollo real — factor de precio/reserva en 4 artículos de `travel` (madrid-travel-guide, spain-tour-1week, budget-spain-tours, spain-budget-solo), FAQ práctica de `spain-map/sevilla`, ejemplo cultural suelto en 2 artículos de `language`, y color de entrevista en 3 artículos de `living` (incluida una sección propia en `chizuyo-in-sevilla.mdx` sobre japonesas que aprenden flamenco en Sevilla — angle de comunidad, no informativo). Se añadieron enlaces recíprocos hacia `flamenco.mdx` en todos esos puntos en vez de expandirlos ahí.

**Estudio de canibalización previo a `traditions.mdx` (2026-09-16):** de las 18 fiestas, 6 ya tenían alguna mención en el sitio (Fallas en `spain-map/valencia` FAQ + `budget-spain-tours` + una entrevista de `living`; Feria de Abril en `spain-map/sevilla` FAQ + `spain-tour-1week`; San Fermín y Semana Santa en `budget-spain-tours`/`spain-tour-1week`/`spanish-vocabulary`). Ninguna era un desarrollo real — todas eran factor de precio, aviso logístico, anécdota personal o contexto de vocabulario. Se añadieron enlaces recíprocos hacia `traditions.mdx` en esos puntos en vez de expandirlos ahí, dejando a `traditions.mdx` como único dueño de la intención informativa "qué es / cuándo es / qué pasa" para cada fiesta.

**Bug de rutas corregido en el mismo cambio:** los catch-all `[cluster]/[article]` de `culture`, `living` y `travel` no excluían los artículos insignia (slug === cluster) — el mismo bug ya corregido antes en `food` y `language`. En `culture` y `travel` era latente (sin insignia activo aún); en `living` **era un duplicado real en producción** (`/living/cervantes-webinar-experiencias-espana/cervantes-webinar-experiencias-espana/`), aunque ya mitigado con una lista hardcodeada — se generalizó al mismo patrón genérico `article !== cluster` que usan food/language.

**`ProvinceRelated.astro` generalizado:** el campo `relatedFood` resolvía solo contra artículos del pillar `food`; ahora resuelve contra toda la colección `articles` (cualquier pillar), con icono por pillar (`food`→UtensilsCrossed, `culture`→PartyPopper, resto→BookOpen) — necesario para que las tarjetas de provincia puedan enlazar a `/culture/traditions/`.

### football

| URL real | Estado | Keyword principal | Volumen (bucket) | Fuente del volumen |
|---|---|---|---|---|
| `/football/` | publicado (hub) | スペイン サッカー | 50k (150k agregando 2 KW secundarias) | `Keywords_Fútbol.csv` |
| `/football/el-clasico/` | publicado (artículo, insignia) | レアル マドリード バルセロナ | 5k (nodo: 22k) | `Keywords_Fútbol.csv` |
| `/football/fcbarcelona/` | publicado (artículo, insignia) | fc バルセロナ | 500k (nodo: 566k) | `Keywords_Fútbol.csv` |
| `/football/real-madrid/` | publicado (artículo, insignia) | レアル マドリード リーグ | 5k (nodo: 15k) | `Keywords_Fútbol.csv` |
| `/football/la-liga/` | publicado (artículo, insignia) | ラ リーガ | 500k (nodo: 505.5k) | `Keywords_Fútbol.csv` |
| `/football/spain-national-team/` | publicado (artículo, insignia) | スペイン 代表 | 500k (nodo: 555.5k) | `Keywords_Fútbol.csv` |
| `/football/japan-vs-spain/` | publicado (artículo, insignia) ★prioritaria en el plan | スペイン 日本 サッカー | 5k (nodo: 6k) | `Keywords_Fútbol.csv` |
| `/football/laliga-master/` | publicado (artículo, insignia) | サッカー 大学 | 5k (nodo: 10k) | `Keywords_Fútbol.csv` |
| `/football/spanish-vocabulary/` | publicado (artículo, insignia) | スペイン 語 サッカー 用語 | 500 (nodo: 2k) | `Keywords_Fútbol.csv` (bajo el nombre plan `spanish-football-terms`) |
| `/football/ferran-torres/` | publicado (artículo, insignia) | — | N/D | N/D — no aparece en `Keywords_Fútbol.csv` |
| `/football/japanese-players-laliga/` | publicado (artículo, insignia) | — | N/D | N/D |
| `/football/liga-f-women/` | publicado (artículo, insignia) | — | N/D | N/D |
| `/football/valencia-cf/` | publicado (artículo, insignia) | — | N/D | N/D |
| `/football/world-cup-2026/` | publicado (artículo, insignia) | — | N/D | N/D |
| `/football/football-coach-license/` | publicado (artículo, insignia) | — | N/D | N/D |
| `/football/lamine-yamal/` | publicado (artículo, insignia) | — | N/D | N/D |

**Football es el único pillar sin stubs** — los 15 clústeres tienen artículo publicado. También es el único pillar **ausente por completo del xlsx maestro** (que solo cubre language/travel/living/food/culture) — su research vive aparte en `Keywords_Fútbol.csv`, y solo cubre 9 de los 15 artículos reales; el resto se creó sin research documentado.

**Ampliación 2026-09-16 — sección "現地観戦ガイド" de `/football/la-liga/`:** a partir del análisis de una guía turística escaneada (`Escaneo/informe_completo_LaLiga.md`), se amplió esta sección (antes 3 párrafos breves) con: cómo comprar entrada (web oficial/taquilla/agencia-hotel), compra de merchandising antes del día del partido, logística del día (llegar con tiempo, comida limitada dentro del estadio, equipaje mínimo, ropa según clima) y etiqueta/seguridad (evitar colores del rival, respetar a la afición local). **Datos del informe descartados por obsoletos y NO usados**: lista de 20 clubes de temporada 2023-24 (el artículo ya tenía la lista real 2026-27), aforos de Bernabéu/Camp Nou (~75k/~98k en el informe vs. ~83.186 actual y ~62.600→105.000 en obras, ya documentados correctamente en `real-madrid.mdx` y `fcbarcelona.mdx`), y el rango de precios €20-170. Ningún artículo nuevo — el hueco era de profundidad dentro de una sección ya existente con la misma intención de búsqueda, no de cobertura ausente.

### Páginas de conversión planificadas — nunca construidas

El xlsx maestro define un nodo `/services/` explícitamente marcado "SEO対象外" (no compite por keywords propias, recibe tráfico desde los clusters informativos): `/services/spanish-lessons/`, `/services/travel-consulting/`, `/services/study-abroad-support/`. **Ninguna de las tres existe en el sitio real** — no hay ningún `src/pages/services/` hoy. Anotado aquí para que no se pierda de vista si se retoma esa capa de conversión.

## Casos especiales a documentar explícitamente

**`/food/spain-map/[province]/` — colección aparte, no artículos.** Vive en la colección `spain-map` (`src/content/spain-map/`, schema propio en `content/config.ts`), no en `articles`. 53 páginas. Confirmado archivo por archivo (ver `barcelona.mdx` como referencia completa): en general **no contienen nombres de restaurantes ni negocios concretos** — es overview editorial genérico (platos típicos, barrios, FAQ) más un botón de salida (`ProvinceMapEmbed.astro`, `<a href={mapUrl} target="_blank">`, **no es un iframe embebido**) hacia un mapa de Google Maps que el copy del propio sitio describe como "curado por スペイン倶楽部" — 5 de las 53 provincias (madrid, barcelona, sevilla, valencia, pais-vasco) son guías editoriales completas creadas el 2026-05-24 en una sesión anterior con Claude; las otras 48 son entradas ligeras creadas el 2026-09-07 en esta sesión. **Diferénciar claramente esta página de cualquier futura página de rankings de restaurantes reales** (el artefacto CRE — `<CRECard>`, ver sección CRECard de `CLAUDE.md` — sí muestra negocios con nombre, rating y reseñas; `spain-map` no).

**Primera excepción (2026-09-16):** `madrid.mdx` ahora sí nombra 3 restaurantes reales (Botín, La Bola, Lhardy) en una sección "歴史あるレストランを訪れる", a partir del análisis de una guía turística escaneada. Es una mención editorial de establecimientos históricos con contexto (no un ranking con rating/reseñas como el artefacto CRE), y encaja con la regla food=dónde comer — se decidió aquí en vez de en `madrid-travel-guide.mdx` (travel=qué visitar) precisamente por esa regla. Si se repite este patrón en más provincias, actualizar esta nota para reflejar que ya no es una excepción aislada.

**`/travel/barcelona/` vs `/food/spain-map/barcelona/`** (y el mismo patrón para cualquier otra ciudad): mismo nombre de ciudad, dos pillars distintos con intención distinta — `travel` = turismo/qué visitar/sitios; `food` = dónde comer. **Nunca fusionar ni compartir keyword principal entre ambos.**

**Deuda técnica conocida:** `author: supeinclub-henshubu` en las 53 entradas de `spain-map` referencia un autor que **no existe** en `src/content/authors/` (verificado: no hay ningún archivo con ese slug). Inofensivo hoy porque `ProvinceGuideLayout.astro` nunca llama a `entry.data.author` para renderizarlo — pero pendiente de limpiar (apuntar a `david-recalde`, el autor real usado en el resto del pillar food) la próxima vez que se toque `spain-map`.

**`/articles/[...page]` reposicionada como hub de "novedades" (2026-09-16):** antes era un volcado cronológico genérico ("すべての記事") con las 6 páginas indexadas y la misma meta description duplicada en todas — sin enlace desde ningún nav/footer, solo descubierta vía sitemap XML, aunque sí enlazada desde la sección "新着" del home. Se realinea con esa etiqueta del home: título "新着記事一覧｜スペイン倶楽部", H1 y descripción propios, `dateModified` en el schema `CollectionPage` (fecha del artículo más reciente de todo el sitio, no solo de la página actual). Solo la **página 1 queda `index, follow`** — es la única con valor real de freshness; páginas 2+ pasan a `noindex, follow` (siguen paginando/enlazando, pero no compiten en el índice con contenido ya viejo). No se solapa con `/sitemap/` (esa es navegación curada por categoría; `/articles/` es el feed cronológico de novedades) ni con los hubs de pillar (esos targetean su propia keyword de categoría). Pendiente si se retoma: añadir un feed RSS (`/rss.xml`) como mecanismo complementario de freshness — no se implementó en este cambio.

**Feed RSS añadido (2026-09-17):** `src/pages/rss.xml.js` (paquete `@astrojs/rss`, ya instalado) genera `/rss.xml` como archivo estático en el build (site en `output: 'static'`, sin coste de rendimiento en runtime) — mismo criterio de ordenación que `/articles/` (todos los artículos no-draft, por `pubDate` descendente). Enlazado como metadato puro (`<link rel="alternate" type="application/rss+xml">` en `BaseLayout.astro`, sin botón visible en la UI — decisión explícita: la audiencia humana del sitio no usa lectores RSS, el objetivo es exclusivamente facilitar el descubrimiento de contenido nuevo a crawlers/agregadores/sistemas de IA) y referenciado también en `public/llms.txt`. Complementa (no sustituye) a `sitemap-index.xml`: el sitemap lista todas las URLs, el RSS destaca específicamente qué es nuevo y cuándo.

**Imágenes añadidas a los 6 artículos sin imagen del 2026-09-16 (2026-09-18):** `traditions.mdx`, `flamenco.mdx` y `souvenirs.mdx` recibieron su `heroImage` (generadas por IA, fotorrealistas, 1.91:1 salvo `hero-flamenco.webp`/`hero-souvenirs.webp` que salieron en 4:3 por defecto del generador — visualmente correcto igualmente, el ratio exacto no es estricto en el sitio); además 12 imágenes de cuerpo repartidas entre esos 3 artículos más `madrid-travel-guide.mdx` y `spain-map/madrid.mdx` (2 y 1 respectivamente). Convención confirmada al implementar: `heroImage` vive en `src/assets/hero/` (import de Astro, se re-optimiza en build), las imágenes de cuerpo van sueltas en `public/` (referenciadas con `<img src="/nombre.webp">`, sin procesar por Astro) — mismo patrón que `jamon-iberico.mdx`. `la-liga.mdx` recibió sus 2 imágenes de cuerpo (`grada-metropolitano.webp`, `merchandising-fc-barcelona.webp`) en un cambio previo del mismo lote.

**`souvenirs.mdx` ampliado + convenciones de marca y de imágenes IA (2026-09-18):** copy ampliado con historia/fundación de cada marca (Lladró 1953, Loewe 1846, Carmina 1866, Camper 1975, Pretty Ballerinas 2005/Mascaró 1918, Nanos 1963, Boinas Elosegui 1858 — verificado por búsqueda web antes de escribir, no inventado) y enlaces a sus webs oficiales; nueva sección "免税（タックスフリー）ショッピングについて" (sin solapamiento con ningún otro artículo del sitio, primera vez que se cubre el tema), verificada exhaustivamente contra fuentes oficiales (Agencia Tributaria, BOE) el mismo día tras detectar que el borrador inicial no mencionaba que el umbral mínimo histórico de 90,15€ fue **eliminado** por la Ley 6/2018 (en vigor desde el 5 de julio de 2018) — el folleto PDF oficial de la propia Agencia Tributaria sigue online desactualizado citando ese umbral, así que no basta con una sola fuente oficial si es un documento antiguo; contrastado también con la página web oficial vigente y con una ficha práctica del funcionamiento de los terminales DIVA (deben validarse con la mercancía en mano antes de facturar el equipaje). Enlace a la página oficial de Agencia Tributaria añadido en el cuerpo y en `sources`. **Convención nueva confirmada — enlaces a marcas comerciales**: `rehypeExternalLinks` en `astro.config.mjs` ya aplica `target="_blank"` + `rel="nofollow noopener noreferrer"` a **todo** enlace externo en el cuerpo markdown automáticamente (no requiere sintaxis especial, solo escribir el link markdown normal `[texto](url)`) — verificado en `dist/`. **Convención nueva confirmada — aviso de imagen generada por IA**: `ArticleLayout.astro` ya envuelve automáticamente cualquier `<img>` suelto en un `<figure>` con `<figcaption>` usando el `alt` como texto (ver script al final del layout) — para un aviso de IA explícito (distinto del alt descriptivo), envolver manualmente en `<figure><img alt="..."/><figcaption>※この画像はAIで生成したイメージです。記事内容を分かりやすく伝えるための参考画像としてご覧ください。</figcaption></figure>` (el script detecta el `<figure>` ya existente y no lo duplica). Aplicado a los 3 body-images de `souvenirs.mdx`; pendiente aplicar retroactivamente a las imágenes IA de `traditions.mdx`/`flamenco.mdx`/`madrid-travel-guide.mdx`/`spain-map/madrid.mdx`/`la-liga.mdx` si se retoma. **Regla de fuentes (aplicada retroactivamente a 9 artículos)**: nunca describir el origen de una fuente como "escaneada" (`スキャン`/`提供資料のスキャン`) — usar en su lugar `複数の文献資料を基にした編集部のまとめ` (equivalente a "recopilación de varias fuentes bibliográficas").

**`souvenirs.mdx` reposicionado como "souvenirs deluxe" (2026-09-18):** a petición de David, tras analizar 6 artículos externos sobre souvenirs españoles (hiddengem.guide, souvenirspain.com, spain.info, forbes.es, hola.com, yourfamilyinmadrid.com — filtrando solo ítems premium/artesanales, descartando imanes/llaveros/figuritas kitsch por no encajar en el posicionamiento deluxe): eliminada la sección "エル・カガネル" (no encajaba con souvenirs de gama alta) y eliminada por completo la sección "食べ物のお土産について" (el artículo ya no cubre souvenirs alimenticios — ese terreno queda para `spanish-wine.mdx`/`jamon-iberico.mdx`, sin necesidad de un enlace recíproco desde aquí). "革手袋" sustituido por "エスパドリーユ（高級仕立て）" (Castañer, 1927, inventora de la cuña de alpargata junto a Yves Saint Laurent en los 70 — dato verificado, no inventado). Añadidas 2 secciones nuevas en 伝統工芸: "トレドの刃物・剣" (forja Mariano Zamorano, 1951) y "マントン・デ・マニラ" (chal de seda bordada, origen Manila-Cantón-Sevilla). El hueco de エル・カガネル en ファッション・その他の逸品 se cubre con "モード・イビセンカ「アドリブ」" (Ibiza, única moda con denominación de origen de España). Todos los datos históricos verificados por búsqueda web antes de escribir.

**Nuevo artículo `food/souvenirs/souvenirs.mdx` — souvenirs alimenticios (2026-09-18):** creado para recuperar tráfico de `/food/souvenirs/spain-chocolate-gifts/` (artículo eliminado en `1ab2ac8`, redirigía a `/food/` genérico pese a tener impresiones/clics reales en GSC). Reparto de pilares resultante, limpio y sin solapamiento: `travel/souvenirs/` = deluxe/no alimenticio (artesanía, moda, marcas), `food/souvenirs/` = alimenticio (chocolate, turrón, mazapán, azafrán, aceite de oliva premium) — mismo patrón de bifurcación por intención que `/travel/{ciudad}/` vs `/food/spain-map/{ciudad}/`. **Colisión de slug evitada**: ambos artículos usan `cluster: "souvenirs"` (uno en pillar `travel`, otro en `food`) — como `ArticleLayout.astro` resuelve `relatedArticles` por `a.id.includes(slug)` (substring, no exact-match — ver línea ~49), un `relatedArticles: ["souvenirs"]` a secas matchearía ambos artículos; la convención usada (ya existente en el código, ver `jamon-iberico.mdx` → `"food/spanish-vocabulary"`) es cualificar con el pillar cuando el cluster es ambiguo: `"food/souvenirs"` / `"travel/souvenirs"`. **Redirect actualizado**: `/food/souvenirs/spain-chocolate-gifts/` en `public/_redirects` ahora apunta a `/food/souvenirs/` (antes a `/food/`) para no desperdiciar el tráfico. **Interlinking recíproco añadido**: `travel/souvenirs/` ↔ `food/souvenirs/` (intro de ambos + `relatedArticles`), `food/jamon-iberico/` y `food/spanish-wine/` → `food/souvenirs/` (cierre + `relatedArticles`), `spain-map/alicante.mdx` (turrón) y `spain-map/toledo.mdx` (mazapán) → `relatedFood` añadido. Cluster `souvenirs` añadido a la categoría "名物料理・食材ガイド" de `/food/index.astro`. Todos los datos de marca (Cacao Sampaka 2000, Enric Rovira 1993, Chocolates Valor 1881, Torrons Vicens 1775, Mazapán Santo Tomé 1856, Castillo de Canena 1780, D.O./IGP de Turrón de Jijona-Alicante y Azafrán de la Mancha) verificados por búsqueda web antes de escribir. **Pendiente detectado (no resuelto en este cambio)**: existe un hub vacío y noindexado en `/food/olive-oil/` (`src/pages/food/olive-oil/index.astro`, cluster `olive-oil` sin ningún artículo aún) pensado para una futura guía general de tipos/selección de aceite de oliva — distinto en ángulo del nuevo apartado "プレミアムオリーブオイル" de `food/souvenirs.mdx` (que es sobre marcas premium para regalo), pero mismo campo semántico: cuando se cree contenido en `food/olive-oil/`, diferenciar claramente el enfoque (selección/uso vs. marcas/regalo) y enlazar entre ambos.

**Sección "SNS" en la home (2026-09-17):** nueva sección 03 en `src/pages/index.astro` (特集 pasa de 03 a 04), muestra el artículo destacado en la última publicación de Instagram/X/Facebook (@supeinclub) — resuelve el problema de "link in bio" de Instagram, donde no se puede enlazar directamente a un artículo. Mecanismo: campo `socialFeatured: z.boolean().default(false)` en el schema de `articles` (paralelo al ya existente `featured` que alimenta 特集); se selecciona el artículo más reciente con `socialFeatured: true` (`allArticles.filter(...).sort(...)[0]`). Actualización manual por artículo cada vez que se publica un carrusel — David indica qué artículo, se le pone `socialFeatured: true` en su frontmatter (y se retira del anterior; invariante: solo uno activo a la vez). Mismo layout de card que 特集 (hero a sangre completa + resumen) para consistencia visual, pero diferenciado con eyebrow propio: número "03" + "@supeinclub" + iconos de Instagram/X/Facebook (mismos SVG que `Footer.astro`) en color de marca de Instagram (#C13584), sin botón de "seguir" (la card entera enlaza al artículo, no al perfil). Ejemplo usado en el primer despliegue: `food/jamon-iberico`.

**`travel/souvenirs.mdx` ampliado — tejas/azulejos, cerámica regional y taracea (2026-09-18):** a petición de David, con 4 URLs externas como referencia (artesur.es, amazon.es, realfabrica.com, ceramicaramblena.com). Nueva sección "装飾タイル（アスレホ）" bajo リヤドロ, sobre azulejos pintados a mano de estilo sevillano y tejas conmemorativas personalizadas (nombres, aniversarios, profesiones) — conectada con la tradición granadina de placas de nombres de calle en cerámica Fajalauza. "地域ごとの陶器" ampliada con datos verificados por región (Fajalauza/Granada, Muel/Zaragoza, La Rambla/Córdoba, Manises/Valencia) y enlace a Real Fábrica como tienda que cura piezas de varias regiones a la vez. **Corrección de un error previo**: la sección decía "フォルニェス焼" para Granada — no existe tal nombre; el término correcto, verificado, es **Fajalauza** (esmalte azul cobalto y blanco predominante, no verde como se había escrito). "タラセア" ampliada con productos concretos (joyeros, tableros de ajedrez, bandejas, marcos), maderas nobles usadas (palosanto, sicomoro, nogal) y enlace a TaraceaGranada como tienda de referencia. Eliminada de `sources` la entrada "「¡Paraíso de las compras en España!」特集ページ" a petición de David — ya no debe aparecer como fuente en este artículo (verificar que no reaparezca en futuras ediciones).

**`travel/souvenirs.mdx` — más referencias y 7 imágenes (2026-09-18):** añadidas menciones a Art Escudellers (tienda de Barcelona en la histórica Carrer Escudellers, fundada en 1997 — año verificado por búsqueda web tras detectar y corregir un dato inventado en el primer borrador, "1941", nunca publicado), Aceros de Toletum (cuchillería/espadas de Toledo) y las abarcas menorquinas (calzado tradicional de Menorca, distinto de la alpargata — cuero + suela de goma en vez de esparto), con enlace a la ficha de producto de Real Fábrica. 7 imágenes IA añadidas (con figcaption de aviso): `fabrica-lladro-valencia.webp`, `azulejos-en-sevilla-plaza-espana.webp`, `acero-toledano.webp`, `manton-de-manila.webp`, `taracea-granada.webp`, `boinas-elosegui.webp`, `abarcas-menorquinas.webp` — quedan sin imagen únicamente ダマスキナード y カルミナ/ロエベ/カンペール/プリティ・バレリーナス/エスパドリーユ (comparten la imagen de cabecera de 革製品・シューズ).

**Corrección de atribución de imágenes en `travel/souvenirs.mdx` (2026-09-18):** David señaló que 3 de las 7 imágenes añadidas ese día (`boinas-elosegui.webp`, `taracea-granada.webp`, `abarcas-menorquinas.webp`) no eran generadas por IA sino extraídas directamente de las webs de terceros mencionadas en el artículo (Boinas Elosegui, TaraceaGranada, Real Fábrica) — riesgo real de copyright de fotografía de producto ajena, no solo una etiqueta incorrecta. Consultado con David, decisión: mantenerlas con atribución explícita ("画像提供：[marca]" en `alt` y `figcaption`, sustituyendo el aviso de IA que no aplicaba) en vez de sustituirlas por versiones generadas por IA. **Regla derivada — antes de aplicar la convención de figcaption de aviso de IA a una imagen nueva, confirmar primero si es realmente generada por IA o si procede de una fuente externa** (en cuyo caso corresponde atribución, no el aviso de IA). También se redujo el tamaño de 3 imágenes (`acero-toledano.webp`, `boinas-elosegui.webp`, `abarcas-menorquinas.webp`) con la clase `.img-vertical` (`max-width: 45%`, centrada) ya existente en `global.css` — no todas las imágenes de un artículo necesitan ir a ancho completo.

**`travel/souvenirs.mdx` — 7 imágenes más + romaji bajo las imágenes (2026-09-18):** confirmado con David antes de implementar (siguiendo la regla derivada del incidente anterior) el origen exacto de cada imagen: `camper.webp`, `carmina.webp`, `loewe.webp`, `nanos.webp`, `adlib-ibiza.webp` son fotos reales de las webs de esas marcas (atribución `画像提供：[marca]`, sin aviso de IA); `damasquinado.webp` es una foto real pero de origen no recordado por David (sin atribución de marca ni aviso de IA — se usa solo como imagen ilustrativa, caption neutro); `souvenirs-flamenco.webp` sí es generada por IA (aviso estándar). Todas las nuevas en tamaño reducido (`.img-vertical`) salvo indicación contraria. **Nueva convención — romaji bajo las imágenes**: a petición de David, cada `figcaption` incluye ahora el término original en español entre paréntesis (p. ej. `（azulejo）`, `（mantón de Manila）`, `（damasquinado）`) para que el lector japonés sepa escribir el término en el alfabeto latino — aplicado retroactivamente a todas las imágenes ya existentes del artículo. Cuando el caption ya es una marca en alfabeto latino (LOEWE, Camper, Carmina, Nanos) no hace falta duplicarlo. **Imagen agrandada**: `acero-toledano.webp` (sección トレドの刃物・剣) vuelve a ancho completo (sin `.img-vertical`) a petición de David.

**Nueva clase `.img-medium` (2026-09-18):** David consideró que el contraste entre imágenes a ancho completo y `.img-vertical` (45%) resultaba demasiado brusco/aleatorio visualmente en `souvenirs.mdx`, con 9 imágenes pequeñas. En vez de subir el `max-width` de `.img-vertical` (usada también en `jamon-iberico.mdx` y 6 artículos de fútbol, para fotos de retrato — cambiarlo ahí habría alterado su composición sin que se pidiera), se creó una clase hermana `.img-medium` (`max-width: 65%`, mismo comportamiento responsive) en `global.css`, y se migraron las 9 imágenes pequeñas de `souvenirs.mdx` a esa clase. Usar `.img-medium` por defecto para imágenes que deban verse más pequeñas sin resultar diminutas en el resto del sitio; reservar `.img-vertical` (45%) para fotos claramente verticales/retrato.

**FAQ añadidas a partir de un mapa de "preguntas relacionadas" (2026-09-18):** David compartió una captura de un árbol de preguntas relacionadas (tipo AlsoAsked) con raíz en "スペインお土産完全ガイド" — coincide con el título de nuestros artículos, señal directa de intención de búsqueda real. Del análisis: "スペインで買うべきブランドは？" aparecía repetida 3 veces (la señal más fuerte) → nueva FAQ en `travel/souvenirs.mdx` resumiendo las marcas del artículo; "スペインでしか買えないお土産のお菓子は？" → nueva FAQ en `food/souvenirs.mdx` sobre turrón/mazapán/chocolate como exclusivos de España. Descartadas por fuera de alcance: "スペインのお土産を日本で買うなら何がいい？" (compra en Japón, no en España — intención distinta a la de estos artículos) y la rama de "スペインに行くときの必需品は？" (NG conductas, palabras a evitar, sanitarios — posible artículo futuro de consejos de viaje, no una mejora de souvenirs).

**Roadmap SEO basado en GSC — Prioridades 1-4 ejecutadas, 5 replanteada (2026-09-20):** a partir de un informe SEO externo, contrastado contra el export real de Google Search Console (`SEO/supein.club-Performance-on-Search-2026-09-20.xlsx`) antes de actuar.

- **P1 — Anclas de El Clásico**: Google indexaba 6 H2 del artículo `el-clasico.mdx` como resultados de búsqueda independientes (anchor fragments `#...`), sumando 15.789 impresiones y 0 clics. Extraídos "歴代対戦成績" → `football/el-clasico/head-to-head.mdx` y "メッシ対ロナウド" → `football/el-clasico/messi-vs-ronaldo.mdx` (nuevos sub-artículos, mismo patrón que `schedule.mdx`, que ya demuestra que la extracción funciona: 2,75% CTR / posición 3.2 frente al 0% de las anclas). En el artículo principal, esas dos secciones quedan como teaser corto + enlace (mismo patrón ya usado para "次のエル・クラシコはいつ？" → `schedule.mdx`). Las secciones "起源" y "政治的・文化的背景" se dejan igual — no tienen cluster de búsqueda propio en GSC, forzarlas a página aparte sería fragmentación sin demanda real. **Corrección tras pregunta de David sobre canibalización**: el title/description originales de `el-clasico.mdx` reutilizaban literalmente la misma frase que `head-to-head.mdx` ("通算264試合は106勝106敗52分の完全な互角") — duplicado real detectado antes de publicar. Reescrito el title/description de `el-clasico.mdx` para enfocarse en su ángulo propio (origen, contexto político-cultural, nombres 名勝負), dejando la cifra exacta de récord como propiedad exclusiva de `head-to-head.mdx`.
- **P2 — Desambiguación de marca**: auditado todo `src/` y `public/` en busca de スペインクラブ (katakana) — no se encontró ningún uso, ya estaba limpio. Añadido `Organization` schema en la home (`@id: https://supein.club/#organization`, `alternateName: ["Club Español en Japón", "supein.club"]`, `sameAs` a las 5 redes sociales) — antes no existía y el `AboutPage` schema de `/about-us/` ya referenciaba ese `@id` sin que existiera, quedando huérfano. Añadida nueva sección "スペイン倶楽部とは？" al principio de `/about-us/` con respuesta directa (patrón `〜とは？` ya usado en el resto del sitio).
- **P3 — Páginas de club**: investigadas las conexiones con jugadores japoneses de los 3 clubs con impresiones significativas en GSC (Villarreal, Valencia, Sevilla). Villarreal (久保建英, cesión 2020-21) y Valencia (佐藤龍之介 primer japonés en el primer equipo, academia VCF Japón, Peña VCF Nippon) **ya tenían esta cobertura de forma completa** — no se tocaron. Sevilla no tenía ninguna mención; añadida sección "日本人選手との関わり" con 指宿洋史 (2011, debut en el derbi sevillano) y 清武弘嗣 (2016, fichado del Hannover 96, debut en la Supercopa de Europa vs Real Madrid) — todo verificado por búsqueda web antes de escribir. No se tocaron los títulos (D1): los 3 ya tienen un ángulo diferenciador propio y forzar "日本人選手" en el título habría sido keyword stuffing sin necesidad real.
- **P4 — Cluster La Liga**: `la-liga-guide` no es un cluster duplicado — es una URL antigua que ya redirige a `/football/la-liga/` (mismo patrón que `food/souvenirs/spain-chocolate-gifts`), su rendimiento en GSC es señal heredada en consolidación. Añadida FAQ "ラリーガ（ラ・リーガ）とは何ですか？" con respuesta directa a la query exacta.
- **P5 — Hub `/football/`** (replanteada, no ejecutada como se pedía originalmente): el informe externo pedía "ejecutar el pillar de 2.500-3.000 palabras" asumiendo que no existía — en realidad `/football/index.astro` ya es un hub completo: título ya alineado (`スペインサッカー入門｜リーグ・代表・クラブ解説`), `CollectionPage`+`ItemList` schema, `FAQPage` schema con 7 preguntas, tabla de 20 clubes, y 8 secciones H2 que resumen y enlazan a los artículos flagship correspondientes. Forzar 2.500-3.000 palabras de prosa original aquí duplicaría contenido ya cubierto en profundidad en `la-liga.mdx`, `el-clasico.mdx`, `spain-national-team.mdx`, etc. — rompería el patrón hub-and-spoke usado en todo el sitio. La posición baja (10.0) para una keyword tan competida como「スペインサッカー」probablemente responde a autoridad de dominio/enlaces, no a falta de contenido. No se tocó; recomendación pendiente de discutir con David antes de invertir más aquí.

**`messi-vs-ronaldo.mdx` — corrección de dato y datos nuevos (2026-09-20):** a petición de David, contrastado contra michelacosta.com/messi-vs-cristiano/el-clasico/ (sitio especializado en estadísticas comparativas). Corregido: la asistencia de Ronaldo en el Clásico eran 3 según la fuente inicial, pero michelacosta.com y una segunda búsqueda de verificación coinciden en **1 asistencia** — corregido antes de publicar (el dato de 3 no llegó a estar en producción). Añadido dato nuevo verificado: récord en finales (Messi 5 finales/3 victorias/6 goles/2 asistencias; Ronaldo 4 finales/1 victoria/5 goles/0 asistencias).

**Segunda revisión de canibalización el-clasico/head-to-head (2026-09-20):** David pidió una revisión más exhaustiva (no solo title/description) tras la corrección anterior. Encontrado un duplicado exacto de pregunta+respuesta en el `faq` frontmatter de ambos artículos ("エル・クラシコの対戦成績はどちらが上ですか？" con la misma respuesta palabra por palabra) — eliminada esa entrada de `el-clasico.mdx` (el teaser del cuerpo ya cubre el dato y enlaza a `head-to-head.mdx`, así que era redundante incluso dentro de la propia página). Revisadas también las FAQ de `messi-vs-ronaldo.mdx` y `schedule.mdx` contra las otras dos: las cifras que se repiten (Messi 26/Ronaldo 18 goles) son una restatement natural en preguntas distintas con redacción distinta, no copia literal — no requieren cambio.

**Tercera revisión de canibalización: H1 duplicado (2026-09-20):** David seguía preocupado por que Google no distinga las 3 páginas. Encontrado: `el-clasico.mdx` y `head-to-head.mdx` usaban literalmente la misma palabra "全記録" en su H1 (`h1` del frontmatter). Corregido el H1 de `el-clasico.mdx` a "エル・クラシコの歴史｜起源・背景・名勝負" (ya no reclama ser "el registro completo", que ahora es territorio exclusivo de `head-to-head.mdx`). De paso, se detectó que `el-clasico.mdx` tiene una línea `# ...` suelta al inicio del cuerpo del artículo (fuera del frontmatter `h1`) que duplicaba el H1 antiguo — actualizada para que coincida con el nuevo. **Hallazgo más amplio, no resuelto en este cambio**: esta línea `# ` suelta que repite el H1 en el cuerpo es un patrón preexistente en **82 archivos** del sitio (football, language/learn-spanish, travel/barcelona, travel/madrid) — no es un bug introducido en esta sesión, es una convención ya establecida de antes. Generaría técnicamente 2 elementos `<h1>` por página en todos esos artículos. No se ha tocado por ser un cambio de gran alcance fuera del pedido puntual de hoy — pendiente de decidir con David si merece una limpieza site-wide en otra sesión.

**Reversión de `head-to-head.mdx` (2026-09-20):** tras tres rondas de revisión de canibalización, David siguió sin ver claro el caso de negocio de esta página separada — con razón: a diferencia de `schedule.mdx` (fecha del próximo partido, intención claramente distinta y sensible al tiempo) o `messi-vs-ronaldo.mdx` (tema con identidad propia, la rivalidad de dos jugadores), el historial de resultados es simplemente un dato dentro de "qué es El Clásico", no una intención de búsqueda suficientemente diferenciada. Decisión: fusionar `head-to-head.mdx` de vuelta en `el-clasico.mdx` como sección completa (tabla de récord, desglose por competición, máximos goleadores) — el artículo principal vuelve a reclamar el título/H1 "全記録" (registro completo) al ser de nuevo su único dueño. Eliminado `head-to-head.mdx`, añadido redirect 301 `/football/el-clasico/head-to-head/` → `/football/el-clasico/` (la página había llegado a estar en producción brevemente). `messi-vs-ronaldo.mdx` se mantiene como página separada — su caso sigue siendo sólido y no se ha cuestionado. Lección para el futuro: antes de extraer una sección a página propia, comprobar que la intención de búsqueda es genuinamente distinta (como fecha vs. historia), no solo que Google la esté indexando como anchor fragment — eso último puede pasar con cualquier H2 sustancial, no implica automáticamente que merezca URL propia.

**Corrección: `head-to-head.mdx` no se había eliminado realmente de Git (2026-09-20):** al hacer el commit de reversión (`c5a433d`), el `git add` con lista explícita de archivos no incluyó la ruta del archivo eliminado — el `rm` fue solo a nivel de disco, nunca se comiteó como `git rm`. Resultado: el commit fusionado en el PR #315 seguía teniendo `head-to-head.mdx` (con su contenido antiguo) en el árbol de Git, aunque ausente en el disco local por una peculiaridad de `git checkout` (no restaura un archivo borrado sin comitear si el contenido del blob no cambia entre ramas). **Impacto real verificado: ninguno** — Astro construye desde el sistema de archivos, no desde Git, así que la página nunca se generó en ningún build; y en producción, la regla de `_redirects` intercepta la petición en el borde de Cloudflare antes de que importe el estado del archivo (confirmado con `curl -I` contra supein.club: 301, no la página). Corregido con un `git add` explícito de la ruta eliminada en este mismo commit. **Lección**: al eliminar un archivo, usar `git rm` o `git add -A`/`git add <ruta-borrada>` explícitamente — un `git add` con lista de archivos que omite una ruta borrada no la comitea como eliminada, se queda en un estado fantasma.

**`messi-vs-ronaldo.mdx` — imagen movida a hero + 2 fotos de prensa (2026-09-20):** la imagen que estaba suelta en el cuerpo (`/messi-cristiano.webp`) se movió a `src/assets/hero/hero-messi-vs-ronaldo.webp` y pasó a ser el `heroImage` del artículo (antes reutilizaba el hero genérico de `el-clasico.mdx`). Añadidas 2 fotos de prensa reales con crédito explícito (no generadas por IA, confirmado por David antes de implementar): `messi-cristiano-2010.webp` (SIU WU/AFP/Getty Images) y `messi-cristiano-saludo.webp` (Shutterstock), ambas en tamaño `.img-medium` con `figcaption` de crédito fotográfico en vez del aviso de IA.

**`food/souvenirs.mdx` — imagen hero + 6 imágenes de cuerpo (2026-09-20):** hero sustituido por `food-souvenirs-hero.webp` (David proporcionó dos candidatas de portada; se descartó la primera, `food-souvenirs.webp`, ya implementada un commit antes). 6 imágenes de cuerpo, todas reales (ninguna generada por IA, confirmado con David antes de implementar): `cacao-sampaka-bombones.webp`, `enric-rovira-chocolate.webp`, `chocolate-valor-edicion-limitada.webp` (extraídas de las webs oficiales de cada marca, sufijo `-website` en el archivo original), `turron-artesano-vicens.webp` y `mazapanes-santo-tome.webp` (también de las webs oficiales de Torrons Vicens y Santo Tomé) — todas con `figcaption` de atribución `画像提供：[marca]`, sin romaji porque el nombre ya está en alfabeto latino. `azafran.webp` y `aceite-de-oliva.webp` son fotos de banco de imágenes (no de marca, no IA) — caption neutro solo con el término en español, sin atribución de marca ni aviso de IA (mismo criterio que `damasquinado.webp` en `travel/souvenirs.mdx`).

**Enlaces de Google Maps en `spain-map/*.mdx` — bug de "Unsupported link" en la app de iOS (2026-09-20):** David reportó que al pulsar el botón de Google Maps de `/food/spain-map/asturias/` en el móvil (iOS), la app nativa de Google Maps mostraba "El vínculo no es compatible". Diagnóstico: los 53 `mapUrl` son enlaces cortos `maps.app.goo.gl` que apuntan a una **Google Maps "List"** (colección de sitios guardados, formato `/maps/@/data=!3m1!4b1!4m3!11m2!2s<id>!3e3`) — bug documentado en la comunidad oficial de Google Maps: los short-links a "Lists" fallan al entregarse a la app nativa en iOS con ese mismo mensaje, aunque el enlace resuelve correctamente en navegador. No es un problema de nuestro código (`HeroProvince.astro` y `ProvinceMapEmbed.astro` usan `<a href={mapUrl} target="_blank">` normal). **Fix aplicado**: resueltos los 51 short-links funcionales a su URL larga (`https://www.google.com/maps/@/data=...` o `.es` según el caso) vía `curl`/redirect, sustituyendo el `mapUrl` en cada `.mdx` — reduce un salto de redirección, mitigación documentada oficialmente (no elimina garantizado el bug de "List" en todas las versiones de la app, es un problema del lado de Google con ese formato). **Hallazgo colateral, no resuelto**: los short-links de `ceuta.mdx` y `melilla.mdx` devuelven **404** — están rotos, no solo incompatibles con la app, ni siquiera resuelven en navegador de escritorio. David confirmó que no tiene listas creadas para esas dos provincias — se dejan sin `mapUrl` funcional por ahora, pendiente de crear las listas y actualizar cuando estén disponibles.

**`culture/traditions.mdx` — 13 imágenes nuevas + sustitución de la de Fallas (2026-09-21):** completadas 13 secciones de fiestas que no tenían imagen de cuerpo (タンボラーダ, カーニバル, モロス・イ・クリスティアノス, パティオ祭り, 聖体祭, サン・フアンの火祭り, サンティアゴ祭, フェリア・デ・マラガ, ラ・トマティーナ, リオハの収穫祭, ラ・メルセ祭, エル・ピラール祭, サフラン祭り) — queda sin imagen únicamente サン・イシドロ祭 (Madrid, no se proporcionó archivo). Sustituida además la imagen de ファジャス（バレンシア）por una nueva (`fallas-valencia.webp`, mismo nombre de archivo en `public/`, contenido reemplazado) — no confundir con el `heroImage` del artículo (`hero-traditions.webp`, archivo distinto, no tocado). **Nueva variante de caption para imágenes IA**: a petición de David, las 14 imágenes de este lote llevan `画像はAIで生成した概念的なイメージです。祭りならではの雰囲気を表現するための参考画像としてご覧ください` en vez del aviso estándar usado en otros artículos — mismo propósito (avisar que es IA), redacción ligeramente distinta para enfatizar que son imágenes conceptuales/idiosincráticas de cada fiesta, no fotos documentales. Todas en `.img-medium`. Las 3 imágenes preexistentes del artículo (セマナ・サンタ, フェリア・デ・アブリル, サン・フェルミン祭) no se tocaron — siguen como `<img>` suelto sin `<figure>`/aviso, fuera del alcance de este cambio.

**Píxel de Metricool sin trackear el sitio desde hacía días — causa: `loading="lazy"` (2026-09-21):** David recibió un aviso de Metricool de que no detectaban la web desde hacía días. El píxel (`<img>` de 1×1, `display:none`, en `BaseLayout.astro` justo antes de `</body>`) ya estaba instalado con el hash correcto — el problema era que el commit `161a875` (2026-09-09, auditoría de rendimiento) le había añadido `loading="lazy"`. Al no tener caja en el layout (`display:none`), el navegador nunca lo considera "cerca del viewport", así que con lazy loading la petición de red nunca llega a dispararse — coincide con los ~12 días transcurridos desde ese commit. **Fix**: quitado `loading="lazy"` solo de este `<img>` (el del logo del footer, que sí es visible, se queda igual). **Lección**: nunca aplicar `loading="lazy"` a una imagen de 1×1/`display:none` (píxeles de tracking) — solo tiene sentido en imágenes con caja real en el layout.

## Mantenimiento

Este archivo se actualiza en el mismo commit que crea o mueve contenido
real — no es un plan aparte que se pueda desincronizar. Antes de crear
cualquier URL nueva, consúltalo primero para evitar duplicar intención de
búsqueda con algo que ya existe.
