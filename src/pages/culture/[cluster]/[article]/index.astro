---
import { getCollection } from 'astro:content';
import type { GetStaticPaths } from 'astro';
import ArticleLayout from '../../../../layouts/ArticleLayout.astro';

export const getStaticPaths: GetStaticPaths = async () => {
  const articles = await getCollection('articles', ({ data }) => !data.draft && data.pillar === 'culture');
  return articles
    .map((entry) => {
      const parts = entry.id.replace(/\.(md|mdx)$/, '').split('/');
      return { entry, cluster: parts[1], article: parts[2] };
    })
    // Los artículos "insignia" (slug === cluster) tienen su propia ruta
    // dedicada en /culture/{cluster}/ y no deben duplicarse aquí (mismo
    // fix aplicado a food y language — ver PR de duplicidad de
    // /food/jamon-iberico/jamon-iberico/).
    .filter(({ cluster, article }) => article !== cluster)
    .map(({ entry, cluster, article }) => ({
      params: { cluster, article },
      props: { entry },
    }));
};

const { entry } = Astro.props;
---
<ArticleLayout entry={entry} />
