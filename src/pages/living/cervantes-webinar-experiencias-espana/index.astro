---
import { getCollection } from 'astro:content';
import ArticleLayout from '../../../layouts/ArticleLayout.astro';

const articles = await getCollection('articles', ({ data }) => !data.draft && data.pillar === 'living' && data.cluster === 'cervantes-webinar-experiencias-espana');
const entry = articles[0];

if (!entry) {
  throw new Error('No se encontró el artículo del cluster "cervantes-webinar-experiencias-espana"');
}
---
<ArticleLayout entry={entry} />
