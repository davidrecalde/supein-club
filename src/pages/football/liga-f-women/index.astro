---
import { getCollection } from 'astro:content';
import ArticleLayout from '../../../layouts/ArticleLayout.astro';

const articles = await getCollection('articles', ({ data }) => !data.draft && data.pillar === 'football' && data.cluster === 'liga-f-women');
const entry = articles[0];

if (!entry) {
  throw new Error('No se encontró el artículo del cluster "liga-f-women"');
}
---
<ArticleLayout entry={entry} />
